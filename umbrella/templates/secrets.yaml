apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  labels:
    app: lusha
    chart: {{ .Chart.Name }}
type: Opaque
data:
  username: {{ .Values.mongo.username | b64enc }}
  password: {{ .Values.mongo.password | b64enc }}